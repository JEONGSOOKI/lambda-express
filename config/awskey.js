module.exports.awskey = (serverless) => ({
    dev: {
        access_key: 'your aws access key',
        secret_key: 'your aws secret key'
    },
    prod: {
        access_key: 'your aws access key',
        secret_key: 'your aws secret key'
    }
});